"""Wiktionary synonym dictionaries.

These JSON Lines files are generated by ``scripts/update_wiktionary_dictionaries.py``
from the kaikki.org Wiktionary dumps. Each line is a JSON object::

    {"word": "house", "s": ["home", "building", "dwelling"]}

Origin
------
Data sourced from https://kaikki.org/ ( Wiktionary dumps ).

Licence
-------
kaikki.org data is published under CC BY-SA 3.0 / GFDL.
See https://kaikki.org/ for full licence details.

Files
-----
- ``fr.json`` — French synonyms
- ``en.json`` — English synonyms
- ``de.json`` — German synonyms
- ``es.json`` — Spanish synonyms
- ``it.json`` — Italian synonyms
- ``manifest.json`` — generation metadata (entries, size, date)

Regeneration
------------
From the repository root::

    python scripts/update_wiktionary_dictionaries.py --all

Or for a single language::

    python scripts/update_wiktionary_dictionaries.py --lang fr

Author: dorel14
Version: 1.0.0
"""

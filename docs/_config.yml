---
# Jekyll configuration for GitHub Pages with Just the Docs theme

# Site settings
title: Whoosh-NG Documentation
description: Pure-Python full-text indexing and search library, modernized for 2025+
lang: en
timezone: UTC

# Global dark theme (applies to all pages)
color_scheme: dark

# GitHub Pages: base URL (repo name for project pages)
baseurl: "/whoosh-ng"
#url: "https://dorel14.github.io"

# Build settings
markdown: kramdown
highlighter: rouge
permalink: pretty

# Use remote theme for Just the Docs (GitHub Pages compatible)
remote_theme: just-the-docs/just-the-docs

# Site favicon and logo (served from docs/assets/)
favicon_ico: "/assets/favico.ico"
logo: "/assets/logo.png"

# Just the Docs theme configuration
just_the_docs:
  # Search
  search_enabled: true
  search_preview: true
  heading_break: 360  # include headings up to h3 in search preview

  # Collections navigation
  nav_sort: case_insensitive
  collections:
    en:
      name: English
      nav_fold: true
    fr:
      name: Français
      nav_fold: true

  # Footer
  footer_content: "Whoosh-NG Documentation | Last updated: 2026-07-26"

# Collections for organized content
collections:
  en:
    output: true
    sort_by: nav_order
  fr:
    output: true
    sort_by: nav_order

# Default front matter for markdown files
defaults:
  # Root pages (README.md, etc.)
  - scope:
      path: ""
      type: pages
    values:
      layout: default
  # Collection: English
  - scope:
      path: "_en"
      type: en
    values:
      layout: default
      lang: en
  # Collection: French
  - scope:
      path: "_fr"
      type: fr
    values:
      layout: default
      lang: fr

# Include additional files
include:
  - _pages
  - README.md

# Exclude from build
exclude:
  - archive/
  - .kilo/
  - .git/
  - node_modules/
  - vendor/
  - "*.py"
  - "*.json"
  - "*.yaml"
  - "*.yml"
  - ".env*"
  - "*.log"
  - "__pycache__/"
  - "*.egg-info/"
  - ".venv/"
  - "venv/"
  - Gemfile
  - Gemfile.lock

# Plugins (GitHub Pages supported)
plugins:
  - jekyll-remote-theme
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-feed

# SEO and social
github_username: dorel14
repository: whoosh-ng

# Permalink style (clean URLs)
permalink: pretty

# Kramdown settings
kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    block:
      line_numbers: false
